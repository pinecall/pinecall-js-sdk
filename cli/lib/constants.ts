/**
 * Shared constants — single source of truth for magic values.
 */

/** Default LLM model for agent/dial commands. */
export const DEFAULT_MODEL = "gpt-4.1-nano";

/** Default WebSocket URL for Pinecall server. */
export const DEFAULT_WS_URL = "wss://voice.pinecall.io/client";

/** Default API URL for REST endpoints. */
export const DEFAULT_API_URL = "https://app.pinecall.io";

/** CLI version (matches package.json). */
export const VERSION = "0.2.0";
